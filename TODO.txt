appcontroller to open "Hamlet.txt" on start
full-screen view
application icon
icons for play/pause button
show/hide controls
help book

preference pane:
* foregroundColor
* backgroundColor
* fontFamily

hotkeys:
* space to togglePlayAndPause
* arrow keys for next/previous word

playback:
* show articles with next word, e.g.:
	require 'strscan'
	l = []
	s = StringScanner.new(words)
	w=""
	while (not s.eos?) && (w += s.scan_until(/\S+/).strip)
		if ["a", "an", "the"].index(w.downcase)
			w += " "
			next
		end
		l << w
		w = ""
	end
	
* length of word should determine display time

readFromURL_ofType_error should return a more useful error when it fails:
	http://is.gd/haDJ
